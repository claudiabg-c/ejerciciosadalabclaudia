const math = require("./math");

const a = 30;
const b = 8;
const operationAdd = math.add(a, b);
const operationSubtract = math.subtract(a, b);

console.log(operationAdd);
console.log(operationSubtract);
